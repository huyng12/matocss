type TransformPropertyFunc = (prop: string, value: string) => string;

// Transform CSS property to Atomic CSS name
// Example: `background-color: red` will be transformed to `bg-red`
export const transformProperty: TransformPropertyFunc = (prop, value) => {
  // TODO: Implement this function
  return "";
};
